//! EmployeeCompetency REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{NaiveDate};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{EmployeeCompetencyService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateEmployeeCompetencyDto, UpdateEmployeeCompetencyDto, PatchEmployeeCompetencyDto, EmployeeCompetencyResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum EmployeeCompetencyError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for EmployeeCompetencyError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for EmployeeCompetencyError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "EMPLOYEECOMPETENCY_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "EMPLOYEECOMPETENCY_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEECOMPETENCY_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEECOMPETENCY_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for EmployeeCompetency.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /employee_competencies | List with pagination |
/// | POST | /employee_competencies | Create new |
/// | GET | /employee_competencies/:id | Get by ID |
/// | PUT | /employee_competencies/:id | Full update |
/// | PATCH | /employee_competencies/:id | Partial update |
/// | DELETE | /employee_competencies/:id | Soft delete |
/// | POST | /employee_competencies/bulk | Bulk create |
/// | POST | /employee_competencies/upsert | Upsert |
/// | GET | /employee_competencies/trash | List deleted |
/// | POST | /employee_competencies/:id/restore | Restore |
/// | DELETE | /employee_competencies/empty | Empty trash |
/// | GET | /employee_competencies/:id/deleted | Get deleted by ID |
/// | DELETE | /employee_competencies/trash/:id | Permanent delete from trash |
/// | GET | /employee_competencies/count | Count active entities |
/// | GET | /employee_competencies/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(EmployeeCompetencyService::with_repository(repository));
/// let router = create_employee_competency_routes(service);
/// ```
pub fn create_employee_competency_routes(service: Arc<EmployeeCompetencyService>) -> Router {
    BackboneCrudHandler::<EmployeeCompetencyService, EmployeeCompetency, CreateEmployeeCompetencyDto, UpdateEmployeeCompetencyDto, EmployeeCompetencyResponseDto>::routes(
        service,
        "/employee_competencies",
    )
}

/// Create Axum router with only the read (GET) endpoints for EmployeeCompetency.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_employee_competency_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_employee_competency_read_routes(service: Arc<EmployeeCompetencyService>) -> Router {
    BackboneCrudHandler::<EmployeeCompetencyService, EmployeeCompetency, CreateEmployeeCompetencyDto, UpdateEmployeeCompetencyDto, EmployeeCompetencyResponseDto>::read_routes(
        service,
        "/employee_competencies",
    )
}

/// Create Axum router with only the write (mutation) endpoints for EmployeeCompetency.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_employee_competency_write_routes(service: Arc<EmployeeCompetencyService>) -> Router {
    BackboneCrudHandler::<EmployeeCompetencyService, EmployeeCompetency, CreateEmployeeCompetencyDto, UpdateEmployeeCompetencyDto, EmployeeCompetencyResponseDto>::write_routes(
        service,
        "/employee_competencies",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_employee_competency_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<EmployeeCompetencyService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_employee_competency_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

