//! EmployeeSkill seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial EmployeeSkill data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EMPLOYEESKILL SEEDER
// ============================================================================

/// Seeder for employee_skill
pub struct SeedEmployeeSkillSeeder;

impl SeedEmployeeSkillSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEmployeeSkillSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEmployeeSkillSeeder {
    fn name(&self) -> &'static str {
        "SeedEmployeeSkillSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if learning.employee_skills table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM learning.employee_skills")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/employee_skill_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM learning.employee_skills")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

